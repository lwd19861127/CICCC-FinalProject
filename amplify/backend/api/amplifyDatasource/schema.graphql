enum Status {
  ON
  OFF
}

enum ArticleCategory {
    BookRecommendations
    ParentalExperiences
}

type Article @model {
  id: ID!
  title: String!
  date: AWSDateTime!
  link: String!
  status: Status!
  image: String
  categories: ArticleCategory
  priority: Int
  description: String
}
